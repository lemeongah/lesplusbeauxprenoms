name: Deploy WordPress Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to production via SSH
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: gillesah
          key: ${{ secrets.GILLESAH_SSH_KEY }}
          script: |
            set -e

            # Déterminer le FOLDER_NAME depuis le repository name
            REPO_NAME=$(echo "${{ github.repository }}" | cut -d'/' -f2)
            DEPLOY_DIR="/var/www/$REPO_NAME"

            echo "📦 Préparation du déploiement..."
            mkdir -p "$DEPLOY_DIR"
            cd "$DEPLOY_DIR"

            # Vérifier si le repo git existe déjà
            if [ -d .git ]; then
              echo "🔄 Repository existant - mise à jour..."
              git fetch origin main
              git reset --hard origin/main
              git clean -fd
            else
              echo "📥 Initialisation du repository..."
              git init
              git remote add origin https://github.com/${{ github.repository }}.git
              echo "🔄 Synchronisation du code depuis GitHub..."
              git fetch origin main
              echo "✨ Reset au dernier commit..."
              git reset --hard origin/main
              git clean -fd
            fi

            echo "📊 État du déploiement..."
            echo "  Repository: ${{ github.repository }}"
            echo "  Branch: main"
            echo "  Commit: $(git rev-parse --short HEAD)"

            echo "🚀 Redémarrage des services Docker..."
            cd "$DEPLOY_DIR/project"

            # ⚠️  IMPORTANT: Backup la base de données AVANT le redémarrage
            echo "💾 Création d'une sauvegarde de sécurité..."
            mkdir -p backups
            BACKUP_FILE="backups/backup_before_deploy_$(date +%Y%m%d_%H%M%S).sql.gz"

            # Vérifier si la base de données existe
            if docker compose ps db 2>/dev/null | grep -q "Up"; then
              echo "  → Dumping de la base de données..."
              docker compose exec -T db sh -c "exec mysqldump -u\$DB_USER -p\$DB_PASSWORD \$DB_NAME" | gzip > "$BACKUP_FILE" 2>/dev/null || echo "  ⚠️  Backup échoué (BDD peut être vierge)"
              if [ -f "$BACKUP_FILE" ] && [ -s "$BACKUP_FILE" ]; then
                echo "  ✅ Backup créé: $BACKUP_FILE"
              fi
            fi

            # Arrêter les services mais préserver les volumes (DB)
            docker compose down || true

            # Redémarrer avec reconstruction (préserve les volumes)
            docker compose up -d --build

            echo "✅ Déploiement terminé pour $REPO_NAME"
